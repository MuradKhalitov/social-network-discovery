spring:
  application:
    name: discovery
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: discovery/config/

eureka:
  dashboard:
    path: /dashboard/
    enabled: true
  client:
    enabled: true
    serviceUrl:
#      defaultZone: http://localhost:8888/eureka/
      defaultZone: http://89.111.174.153:8888/eureka/
#      defaultZone: 89.111.174.153:8888
server:
  port: 8888